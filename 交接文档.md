# 订单助手项目 —— 交接文档

> 用途：换电脑继续开发用。本文档记录需求、代码现状、部署进度与接手步骤。
> 创建时间：2026-08-26 · 由开发会话记录整理
> 说明：本文件会随代码一起推送到 GitHub，新电脑拉取后按本文件即可无缝续作。

---

## 1. 项目一句话
一个 3 人团队共享的「校园二手图书配送订单助手」网站：多人实时同步订单、按校区/宿舍编号管理配送、对外分享链接让顾客自助下单。**无登录账号**。

## 2. 需求清单（已完成确认，无需再讨论）
1. 订单表单：配送区域（大苑+编号）、预定配送时间、联系方式（手机/QQ/微信）、备注、书单明细（可增删改）。
2. 订单列表：按预定配送时间由近到远；已完成订单置灰变淡并排到最末。
3. 订单状态：待配送 → 配送中 → 已完成。
4. 配送区域二级联动：
   - 大千苑18栋 → 18-1~18-4
   - 长江苑19栋 → 19-1~19-4
   - 大洲苑21栋 → 21-1~21-4
   - 培伦苑20栋 → 20-1~20-4
5. 书籍名称记忆：输入书名时根据历史书名联想/点选。
6. 统计面板：每种书在所有订单（**含已完成**）中的总数量。
7. 历史订单：已完成保留可查看，支持一键清空。
8. 配送清单导出：按大苑分组打印。
9. **四大配送区域独立页面**：首页 4 个大苑入口卡片（含待配送到肩标），点入只看该苑订单、可按编号筛选，方便专人配送。
10. **移动端优先适配**：手机常用，375px 布局自适应、触控友好。
11. **顾客自助下单页（对外分享）**：独立精简表单页，顾客填单提交后实时进入共享列表。
- 已确认的技术选择：Cloudflare Workers + D1、前端 GitHub Pages、每 4 秒轮询同步、已完成订单保留可清空、统计含已完成。

## 3. 技术架构与目录
```
Order-Assistant/                      # 仓库根目录
├── README.md                         # 部署说明
├── 交接文档.md                        # 本文档
├── .trae/documents/订单助手网站开发需求文档.md  # 完整需求文档（新电脑务必先看这个）
├── frontend/                         # 前端（部署到 GitHub Pages）
│   ├── index.html                    # 后台 SPA（区域配送/订单列表/新建/统计/历史）
│   ├── order-entry.html              # 顾客自助下单页（对外分享链接）
│   ├── css/style.css
│   └── js/app.js
└── worker/                           # Cloudflare Workers 后端
    ├── src/index.js                  # 全部 REST API（绑定 D1，绑定名 DB）
    ├── wrangler.toml                 # D1 配置（database_id 是占位符，需改成真实值）
    └── schema.sql                    # 建表脚本
```

## 4. 后端 API 一览
| 方法 | 路径 | 作用 |
|---|---|---|
| GET | /api/orders | 全部订单(含明细)，按尽快→时间升序，done 恒最末 |
| POST | /api/orders | 新增订单（校验区域/编号/联系方式/明细） |
| GET | /api/orders/:id | 单个订单 |
| PUT | /api/orders/:id | 更新订单+明细 |
| PATCH | /api/orders/:id/status | 更新状态 pending/delivering/done |
| DELETE | /api/orders/:id | 删除订单 |
| POST | /api/clear-done | 清空所有已完成订单 |
| GET | /api/book-names | 去重书名（联想用） |
| GET | /api/stats | 每种书总量（含已完成） |
- 响应结构：`{ok,data}` / `{ok,error}`。绑定名为 `DB`（D1）。

## 5. 当前部署进度（重要）
✅ 已完成：
- 后端代码、前端代码全部写完，JS 语法已校验通过（`node --check`）。
- 尝试在本机安装 wrangler → 已装上（v4.126.0）。
- ⚠️ 卡点：**IDE 内置终端沙箱禁止 wrangler 写入用户目录（登录凭证/日志），无法在 IDE 内完成 Cloudflare 部署。**

❌ 未完成（下一步要做）：
- wrangler login（需浏览器登录）。
- wrangler d1 create order-assistant-db（获取 database_id）。
- 把 database_id 填进 `worker/wrangler.toml`。
- 初始化表：`wrangler d1 execute order-assistant-db --remote --file=./schema.sql`。
- `wrangler deploy`（得到 workers.dev 地址）。
- 把 workers 地址填进前端两处 `WORKER_BASE`：
  - `frontend/js/app.js`（顶部）
  - `frontend/order-entry.html`（顶部 script）
- 前端推到 GitHub Pages 并启用。
- 联调验证。

## 6. 从新电脑接手步骤（照着做）
前提：新电脑装好 Node.js（≥18）与 GitHub CLI 或 Git。

**① 拉代码**
```bash
git clone <你的仓库地址>
cd Order-Assistant
```

**② 装 wrangler 并登录（在新电脑自己的终端，别用受限制的沙箱终端）**
```bash
npm install -g wrangler
cd worker
wrangler login        # 浏览器登录你的 Cloudflare
wrangler d1 create order-assistant-db
```
`wrangler d1 create` 输出中找 `database_id = "xxxx"` 那一串。

**③ 填 database_id**
打开 `worker/wrangler.toml`，把 `database_id = "your-d1-database-id-here"` 替换成上面拿到的真实 id。
> 注意：真实 database_id 相当于数据库钥匙，**不要**提交到公开仓库。可以只在本地改完部署。

**④ 初始化表结构**
```bash
wrangler d1 execute order-assistant-db --remote --file=./schema.sql
```

**⑤ 部署 Worker**
```bash
wrangler deploy
```
记下输出的网址，形如 `https://order-assistant-worker.你的子域.workers.dev`。

**⑥ 填前端 API 地址**
打开 `frontend/js/app.js` 和 `frontend/order-entry.html`，把顶部 `const WORKER_BASE = "https://your-worker.workers.dev";` 改成第⑤步的网址。

**⑦ 部署前端到 GitHub Pages**
- 用 GitHub Pages：在仓库 Settings → Pages，选择 `main` 分支、目录为 `frontend/`，启用。
- 或者任意静态托管，把 `frontend` 目录内容上传。
- 得到两个链接：
  - 后台：`https://你的用户名.github.io/仓库名/index.html`
  - 顾客下单：`https://你的用户名.github.io/仓库名/order-entry.html`

**⑧ 联调验证**
按下面「验收清单」过一遍。

## 7. 验收清单（部署完成后逐条核验）
1. 订单列表能新增/编辑/删除订单。
2. 状态待配送→配送中→已完成可切；完成置灰、排最后。
3. 排序：尽快配送最前，其次按时间近→远。
4. 区域二级联动正确，编号不串苑。
5. 书名联想可点选历史书名。
6. 统计含已完成求和正确。
7. 清空已完成功能正常。
8. 两台设备同时开：一端改单，另一端 ≤4 秒自动更新（轮询同步）。
9. 首页 4 个大苑卡片、点入只看该苑、可筛编号。
10. 手机宽度（375px）不横向溢出、可正常点击。
11. 顾客点分享链接只看到下单表单，提交后后台 4 秒内出现、状态待配送。

## 8. 注意事项 / 坑
- 无鉴权：后端和前端均公开 URL，任何知道地址的人可访问/下单。后台地址请勿外发。
- 不要在 IDE 沙箱终端里跑 wrangler（会因写用户目录被拦截），用系统终端。
- database_id 和真实 workers 地址建议不写入公开仓库；若仓库是私有的则无碍。
- 前端强制刷新：改完 `WORKER_BASE` 后重新部署 Pages（或强刷浏览器缓存）。
- 换电脑后，若有未推送的本地改动，先 `git add .; git commit; git push` 打包带走，新电脑 clone/pull 即可。

## 9. 本机现状补充（给当前这台电脑备份/打包用）
- 项目路径：`E:\practice\AIbox-CN\Order-Assistant`
- 所有代码已在仓库内；若尚未 git init，请在换机前先推送。
- 本会话中仅创建/修改了上述代码文件，未改任何系统配置。