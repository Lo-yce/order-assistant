# 依赖与临时文件
node_modules/
*.log
.DS_Store

# Wrangler 本地运行时
.wrangler/
cloudflare-*.json

# Cloudflare API 令牌（本地私有，严禁提交到仓库）
# 用于 wrangler 认证，从环境变量 CLOUDFLARE_API_TOKEN 读取
*.cf-token
cf-token.txt

# wrangler.toml 中的 D1 database_id 本身无泄露风险（需配合 API 令牌才可操作），
# 为支持 GitHub Actions 自动部署 Worker，将其纳入仓库管理
